# ============================================================
# Configured by Ansible on {{ lookup('pipe','date') }}
# ============================================================
*.*   @{{ cluster_fqdn }}:{{ services.fluentd.ports.syslog.number}}   # UDP

- hosts: cumulus
  strategy: free
  gather_facts: no
  become: true
  pre_tasks:
    - name: Import initial user/pwd for first configuration
      include_vars: includes/initial-cumulus.yml
      when: initial is defined
  tasks:
    - include_tasks: tasks/cumulus/main.yml

{% for host in hosts %}
{% if host.interfaces.management.mac_address %}
host {{ host.hostname }}
{
    option host-name "{{ host.hostname }}.{{ dns_domain }}";
    hardware ethernet {{ host.interfaces.management.mac_address }};
    fixed-address     {{ host.interfaces.management.ip }};
}
{% endif %}
{% endfor %}
